#include <stdio.h>
#include "utils.h"
#include <stdbool.h>

int main (){

    int boolean = false;

    int isGreater = isGreaterThan10(11);
    printf("the number is: %d", isGreater)

    return 0;
}